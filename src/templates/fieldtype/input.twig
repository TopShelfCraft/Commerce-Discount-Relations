{% do view.registerAssetBundle('BeSteadfast\\DiscountRelations\\web\\assets\\DiscountsField') -%}

{% import "_includes/forms" as forms %}

{%- set class = [
	'nicetext',
	'DiscountsField'
]|filter %}

{% set config = {
	id: name,
	name: name,
	value: value,
	class: class,
} %}

{{ forms.text(config) }}
